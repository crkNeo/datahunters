# 資料採集器(collector)

`backend/cmd/collector` —— 每分鐘把幣安 USDT 永續的全市場快照寫進 MySQL,並回填「快照之後發生了什麼」。

這是**研究用的紀錄器,不是交易工具**。它刻意不做任何判斷:沒有分數、沒有閾值、沒有訊號、沒有推播。

## 為什麼先做這個

目標是抓「幾分鐘內 +10~30%」的山寨小幣爆發。這類行情的物理本質是**強制平倉在薄訂單簿上連鎖引爆**,而不是基本面買盤 —— 沒有任何買盤能在五分鐘內把價格推 20%。

所以真正該問的問題不是「哪個指標會亮燈」,而是:

> 在事件發生前,哪些可觀測的量真的動了?而且是**只有**在事件發生前才動?

要回答它,必須有一份「當時的全市場快照 + 之後真的發生了什麼」。事後翻新聞永遠找得到理由(每一次爆發都能找到某個解鎖、某筆巨鯨轉帳),但那是倖存者偏差:你看不到當天同樣有巨鯨轉帳、卻什麼都沒發生的另外 29 隻幣。

**沒有這份資料,調參數只是換一個故事講。**

## 三條設計原則

1. **只存原始值。** 不存 z-score、不存百分位、不存任何「燃料分數」。衍生值離線算,而且只准用 ≤ T 的滾動窗口。把公式凍進採集階段,等於改一次規則就要重收一次資料;更糟的是,之後很容易不小心用「完整歷史」的統計量去算,那就是洩漏未來,任何回測都會說謊。

2. **全市場都記,不只記有訊號的。** 只記訊號成立的幣,算得出「訊號後噴了幾次」,永遠算不出「**沒有**訊號時噴了幾次」。少了對照組就沒有 lift,而沒有 lift 就無法判斷一個分數到底有沒有資訊量。

3. **一開始就按日分區。** MySQL 上用 `DELETE` 清時序表意味著逐列刪除、大量 undo log、而且空間不會還給作業系統。日分區之後保留策略就是 `DROP PARTITION`,瞬間完成且真的釋放空間。等表長到幾千萬列再補分區要整表重建,所以必須建表時就做。

## 執行

```bash
cd backend
MYSQL_DSN='user:pass@tcp(127.0.0.1:3306)/datahunter?charset=utf8mb4' \
  go run ./cmd/collector
```

`MYSQL_DSN` 也可以放在 `backend/.env`(跟 `cmd/server` 讀同一個檔)。它跟儀表板共用資料庫但**不同行程** —— 採集器爆掉或被限速暫停都不會影響網站。

常用參數:

| 參數 | 預設 | 說明 |
|---|---|---|
| `-universe` | 100 | 追蹤幾隻,依 24h 成交額排名 |
| `-workers` | 8 | 逐幣抓取的併發數 |
| `-depth-every` | 5 | 幾分鐘抓一次訂單簿(0 = 關閉) |
| `-depth-limit` | 100 | 訂單簿每邊檔數 |
| `-spot-every` | 1 | 幾分鐘抓一次現貨(0 = 關閉) |
| `-retention` | 60 | 保留天數(0 = 全留) |
| `-event-pct` | 0.10 | 5 分鐘內漲多少算一次「事件」 |
| `-settle` | 10s | 過了整分鐘多久才讀已收線的 K 棒 |
| `-no-labeler` / `-labeler-only` | | 只採集 / 只回填 |

所有幣安請求都走既有 `internal/exchange` 的 lane 機制(pacing、418/429 熔斷、`X-MBX-USED-WEIGHT-1M` 監控)。**如果日誌開始出現大量 `errs(bar=...)`,先調高 `-spot-every` 和 `-depth-every`,再考慮設 `BINANCE_PROXIES` 增加出口 IP。**

## 資料表

| 表 | 內容 | 頻率 |
|---|---|---|
| `snap_1m` | 價量、OI(名目美元)、資金費、mark/index、主動買量 | 每幣每分鐘 |
| `depth_1m` | 買賣一檔、spread、±0.5/1/2% 累積深度美元 | 每幣每 5 分鐘 |
| `spot_1m` | 現貨收盤、成交額、主動買量 | 每幣每分鐘 |
| `regime_1m` | BTC/ETH、總 OI、漲跌家數、中位數報酬、橫斷面離散度 | 每分鐘一列 |
| `universe_1d` | 全部可交易永續的狀態、上市日、成交額排名 | 每天 |
| `labels_1m` | 前向報酬、MFE/MAE、到頂秒數、事件旗標 | 回填 |
| `collector_state` | 進度標記 | — |

幾個欄位值得特別說明:

- **`depth_*`** 是「拉抬成本」項。兩隻 OI 和資金費一模一樣的幣,同樣的強平流量,一隻噴 5%、一隻噴 40%,差別純粹在簿子厚度。這是價量資料**永遠推導不出來**的東西。`truncated=1` 代表回傳的簿子在 ±2% 之前就結束了,外圈是下限而非量測值。

- **`spot_1m`** 存在只為一個比值:現貨量 ÷ 合約量。合約帶量、現貨平躺 = 純槓桿,退得跟漲得一樣快;現貨先動、OI 跟上 = 有真實買盤,撐得住。對幾分鐘的持倉,這決定該追還是該立刻走。

- **`regime_1m.disp`** 是橫斷面離散度。高 = 資金在挑幣,低 = 整個板一起動。在低離散的日子裡,任何「最強幣」排名其實只是在重新排列市場 beta。

- **`universe_1d`** 記錄**每一隻**可交易永續(不只追蹤的 100 隻),這是防倖存者偏差的唯一手段。三個月後做分析時,那些中間被下架的幣必須還看得見 —— 而下架的幣恰好就是最容易出現暴力行情的那一群。

- **`labels_1m.secs_to_peak_15m`** 直接回答那個決定整套架構的問題:**人來不來得及點。** 如果中位數遠低於一分鐘,人工盯盤這條路就宣告死亡,只剩自動化或改抓第二波。

## 已知限制

- **OI 是收線後才讀的即時值**,不是該分鐘的平均。幣安沒有 1 分鐘粒度的 OI 歷史端點(`openInterestHist` 最細 5m 且有延遲),所以請把它理解成「該 K 棒收盤當下的 OI」。
- **清算資料不在這裡。** 現有的 `liquidations` 表由 `cache.LiqTick()` 從 OKX 收集,只涵蓋追蹤清單。幣安的強平只有 WebSocket(`!forceOrder@arr`),要補得另開一條常駐連線 —— 以「軋空是主菜」的定位來看值得做,列為下一步。
- **REST 輪詢有延遲。** 對「先存資料」的階段夠用;真要吃第一波,K 線要改走 WebSocket `kline_1m` 合併串流。
- 磁碟粗估 **每天 100~130 MB**(100 隻,含索引),一個月約 3~4 GB。

## 幾天之後要回答的四個問題

先把驗收標準定死,否則觀察完只會得到「感覺有點準」。

**Q1:機會頻率有多高?**
```sql
SELECT COUNT(*) AS events, COUNT(DISTINCT symbol) AS coins
FROM labels_1m WHERE is_event = 1;
```
如果一週只有個位數,這個策略養不活人 —— 得放寬 `-event-pct` 或換標的池。**這題先答,因為它可能直接否決整個計畫。**

**Q2:lift 是多少?**(唯一真正重要的數字)
燃料分數前 15 名的幣,事件發生率 ÷ 全市場平均發生率。**lift < 2 代表分數沒有資訊量**,該砍掉重想,而不是繼續調權重。

**Q3:事件前 30 分鐘,到底哪些欄位真的動了?**
把所有事件對齊在 T=0 往前看每個欄位的平均軌跡。這一題會直接判定資金費、OI、深度、taker 比誰是領先、誰是同步、誰根本沒動。

**Q4:MFE / MAE 的分佈長什麼樣?**
```sql
SELECT ROUND(mfe_5m,2) AS mfe, COUNT(*) FROM labels_1m
WHERE is_event = 1 GROUP BY 1 ORDER BY 1;
```
在看到這個分佈之前,任何止盈/停損數字都是瞎猜。

## 這階段刻意不做的事

- ❌ 自動下單
- ❌ 進場判斷邏輯
- ❌ 調權重(等 Q3 出來再說)
- ❌ 前端

順序反了會白做工:先有資料,再有結論,最後才有規則。
