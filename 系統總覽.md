# datahunter 系統總覽

自架的加密貨幣合約數據看板。後端 Go(純記憶體、無資料庫)+ 前端 Vue 3 / Vite。
分析對象:Binance USDT 永續合約。

---

## 一、整體架構

```
Binance / OKX / Deribit  ──(公開 API)──►  Go 後端(記憶體快取)  ──/api/*──►  Vue 前端(單頁、分頁切換)
```

- **後端**:`internal/cache`(快取 + 計算)、`internal/scorer`(評分器)、`internal/indicator`(技術指標)、`internal/exchange`(交易所 client)、`internal/api`(HTTP 路由)。
- **持久化(SQLite)**:存入 SQLite(純 Go 驅動 `modernc.org/sqlite`,免 cgo)的有:**訊號紀錄(±20 穿越)**、**模擬倉(紀律+動能狙擊)**(重啟自動載回)、**訂單簿大牆/失衡快照**、**清算事件**(後兩者為「往後累積」以便日後驗證盤口/清算是否領先)。檔案 `datahunter.db`(`DB_PATH` 可覆寫)。**這不是 snapshot**——只把已產生的訊號/監控資料存起來累積。
- **設定檔 `.env`**:啟動時自動載入 `backend/.env`(KEY=VALUE,不覆寫已存在的環境變數;`.env` 已 gitignore)。支援 `PORT`、`COINS`、`DB_PATH`、`TELEGRAM_TOKEN`、`TELEGRAM_CHAT_ID`。
- **Telegram 推播**(選用,`internal/notify`):設好 token+chat id 即啟用,否則自動停用。推播項目:
  - 啟動通知
  - **訊號穿越**(±20):附 OI收縮/順勢 品質標記。**預設關**,設 `ALERT_SIGNAL_CROSS=1` 才開
  - **模擬倉開倉/平倉**(紀律 + 動能狙擊):含備註(點火/進場/TP/SL;結果/損益/持倉)
  - **高影響事件前 ~30 分鐘**(CPI/FOMC,每事件一次)
  - **被帶崩「中」與「高」**(每轉變一次)
- **背景定時更新**(`cmd/server/main.go`):

| 工作 | 間隔 | 內容 |
|------|------|------|
| `Refresh` | 60 秒 | 追蹤幣的方向評分(OI 儀表板) |
| `Radar` | 90 秒 | 全市場爆發雷達掃描 |
| `PaperTick` | 30 秒 | 模擬倉(紀律 + 動能狙擊)開倉/監控/平倉 |
| `Risk` | 60 秒 | 美股/風險背景燈(Yahoo) |
| `OrderBook` | 90 秒 | 訂單簿大牆/失衡(Binance depth)→ SQLite |
| `LiqTick` | 2 分鐘 | 清算事件(OKX)→ SQLite |

---

## 二、資料來源(全部免費公開端點)

| 來源 | 用途 |
|------|------|
| **Binance fapi**(合約) | K 線(含**主動買量** → 真實 CVD)、OI 歷史、資金費率歷史、多空帳戶比、大戶持倉、溢價、全市場 ticker、exchangeInfo(分辨幣/個股) |
| **Binance spot**(現貨) | 現貨 K 線(回測用) |
| **OKX** | candles、當下資金費率、**當下** OI(無歷史) |
| **Deribit** | DVOL 隱含波動率指數(僅 `cmd/backtest` 研究用) |
| **Yahoo Finance** | 美股風險背景:S&P/Nasdaq 期貨、VIX、美元 DXY(即時,需帶 User-Agent) |
| **faireconomy(ForexFactory)** | 經濟行事曆(免費 JSON,無金鑰):高影響 US 事件 CPI/FOMC/NFP… 含 forecast/actual。⚠️ 會限制頻繁抓取,故 30 分鐘才更新一次 |
| **Binance fapi /depth** | 即時訂單簿(1000 檔)→ 大牆/失衡。無歷史,往後累積 |
| **OKX liquidation-orders** | 即時清算事件(免費,真實爆倉價/量/方向)+ ctVal 換算 USD。無深歷史,往後累積 |

> ⚠️ **資料天花板**:Binance 的 OI 歷史 API 上限只有**最近 30 天**(實際抓 ~20-25 天),且是「滾動視窗」。這是為何只能在「單一行情」回測,也是 snapshot 的意義(往未來累積、突破 30 天牆)。

---

## 三、兩套獨立的分數系統(最重要的差異)

整個系統有**兩個完全不同的分數**,服務不同目的,**不要混淆**:

| | **OI 方向評分**(±20) | **爆發雷達 點火分**(≥55) |
|---|---|---|
| 問的問題 | 「**往哪個方向**走?」 | 「**會不會劇烈噴/崩**?」 |
| 性質 | 方向 + 強度(-100~+100) | 爆發強度(0~100,無關方向品質) |
| 範圍 | 追蹤的 ~36 個幣 | **全市場** ~300-400 個幣 |
| 用在 | OI 儀表板、數據訊號、推薦卡、訊號紀錄 | 爆發雷達、模擬倉、動能狙擊單 |
| 核心因子 | OI 1h + CVD + 結構 + 動能 + 費率 + 多空比 | OI 堆積 + 量能爆發 + 鯨魚 + 加速 + CVD |
| 方向判定 | 各因子加權(含反指標修正) | OI 堆積方向(CVD 決勝負) |

---

## 四、OI 方向評分器(±20 系統核心)

`internal/scorer/detail.go`。同一套評分器同時供:OI 儀表板、推薦卡、個股明細、數據訊號——**三者永遠一致**。

### 因子與權重(`DefaultDetailWeights`)

| 因子 | 上限 / 半飽和 | 說明 |
|------|------|------|
| **市場結構**(OI 1h + CVD) | OI 15/1.5、CVD 15/8 | 頭號因子。OI 方向 × 主動買賣壓 |
| **價格結構**(HH/HL) | ±8 | 高點高/低點低的型態 |
| **24H 動能** | 10/2 | |
| **資金費率** | 10/0.05 | **反指標**(費率極正=多頭擁擠→偏空) |
| **多空比**(散戶) | 8/15 | **反指標**(散戶過度做多→偏空) |
| ~~1H 動能~~ | **0(停用)** | 回測證實是**反指標** |
| ~~相對強弱~~ | **0(停用)** | 回測證實是**反指標** |

- **流動性抑制**:`總分 = 原始分 × min(1, 24h量/100M)` — 薄量幣分數往 0 壓(回測:<100M 表現差很多)。
- **門檻**:`|總分| ≥ 20` 才標多/空(`BiasThreshold`);評等 = 總分 / 8(0~10 點)。
- **平滑飽和曲線** `smoothSat`:避免單一因子暴衝。

---

## 五、爆發雷達 點火分(≥55 系統)

`internal/cache/radar.go`。全市場(24h量 > $1M、扣穩定幣)併發掃描(10 workers)。

### 點火分組成(`earlyScore`)

```
量能爆發   clamp((volSpike-1)*6, 0, 40)   ← 最大權重
OI 堆積    clamp(|oiAccum|*1.8, 0, 30)    ← 最強訊號
鯨魚足跡   clamp((whale-1)*1.0, 0, 12)    ← 次要
價格加速   clamp(|accel|*1.0, 0, 12)
CVD       clamp(|cvd|*0.4, 0, 8)          ← 僅當與加速同向才計
× 早期性   clamp(1 - |24h漲跌|/68, 0.3, 1) ← 已噴一大段的降權
```

- **方向**:`OI 堆積 ≥ 0 → 看噴(pump)`,接近 0 時用 CVD 決勝負。**不是用動能判方向**(回測:OI 方向 57% 勝過動能 51%)。
- **進場/出場參考**:進場 = fib 0.382 回檔位、觸發 = 突破位;**TP = 現價 ± 0.618×R、SL = 現價 ∓ 0.5×R**(R = 近期擺盪幅度,回測網格最佳)。

---

## 六、各分頁說明

| 分頁 | 用的分數 | 資料 / 內容 |
|------|---------|------|
| **市場列表**(list) | — | 全市場 ticker,可按量/漲/跌排序 |
| **OI 儀表板**(oi) | ±20 方向評分 | 追蹤幣的評分/方向/品質/OKX%/OI 1h%/CVD%/費率 |
| **數據訊號**(signals) | ±20 方向評分 | 只列 `|評分|≥20` 的可進場訊號;**套用過濾層**(見下) |
| **訊號紀錄**(scorelog) | ±20 方向評分 | 記錄分數**穿越** ±20 的時間點(只記穿越、非啟動即記),供回溯。**存 SQLite + 時間範圍篩選** |
| **爆發雷達**(radar) | 點火分 ≥55 | 全市場潛在噴/崩,含現價、點火分、進場參考。幣/個股分開 |
| **訊號追蹤 / 模擬倉**(paper) | 點火分(紀律) | 自動模擬交易:紀律版。存 SQLite + 時間篩選。每筆顯示**動能燈**(分數+CVD 即時) |
| **動能狙擊單**(gamble) | 點火分(寬鬆) | 自動模擬交易:動能狙擊版(A/B 對照)。存 SQLite + 時間篩選。每筆顯示**動能燈** |
| **精選狙擊單**(premium) | 點火分≥45 + 同向 + 費率燃料 | **對照組**:動能狙擊同門檻,但同時要求 OI/CVD 同向 **且** 費率燃料(多→費率負/空→費率正)。訊號量約 1 成,往前累積驗證「精選層」 |
| **個股**(stocks) | 點火分 | 股票代幣(EQUITY)單獨呈現,不混入幣 |
| **財經事件**(events) | faireconomy | 高影響美國經濟事件(CPI/FOMC/非農)清單 + 倒數 + 實際vs預期 |
| **盤口 / 清算**(flow) | Binance depth + OKX | 訂單簿大牆/失衡板 + 清算事件 feed + 1h 多空爆倉。即時監控、非回測;往 SQLite 累積待驗證是否領先 |
| **個股明細抽屜**(點任一幣) | ±20 方向評分 | 做多/做空依據、**K 線圖(1h/4h/1d)**、評分依據明細、相關幣種 |

### 美股/風險背景燈(頂部常駐 strip)— 風險時段提醒,非回測訊號
頁面頂部(header 下方)**所有分頁都看得到**的一條 strip,資料來自 Yahoo Finance(免費即時):
- **S&P 期貨、Nasdaq 期貨、VIX、美元 DXY**(VIX/DXY 上漲標紅=風險趨避)
- **綜合風險燈**:risk-on / 中性 / risk-off(股市強+VIX降+美元弱=risk-on)
- **美股盤狀態 + 倒數**(盤前/開盤中/已收盤,用 America/New_York 自動處理夏令時)
- **⚠️高影響時段**(美東 08:00–16:00,盤前數據到收盤):此窗口加密最易被美股帶動
- **📅 高影響經濟事件倒數**(faireconomy):CPI / FOMC / 非農… 顯示倒數,釋出後顯示「實際 vs 預期」。唯一能「事前」的——事件前可降風險、預期波動
- **⚠️ 被帶崩 預警**(低/中/高,只警示下行):ES/NQ 急跌 + VIX 升 + 美元強 + 事件臨近 + 紐約時段 + BTC 轉弱 → 中/高時頂部跳紅/黃 banner,附原因 + 建議動作(避新多單/縮部位/收緊止損)。**heuristic 風險燈,非回測訊號**

> 用途:**看盤背景燈**——紐約盤 + VIX 高 + 美股期貨弱時,對多單訊號保守。回測「時段過濾」已佐證紐約盤對訊號最不利。**非可回測的方向 alpha**(同步傳導 + 週/月級,25 天窗驗不出)。

---

## 七、過濾層 / Overlay(套在「方向訊號」呈現上)

兩個**經回測驗證**的過濾,**純前端**、可獨立開關、複合套用。**只影響推薦卡 + 數據訊號的呈現,不改評分器、不碰雷達。**

> **開關位置**:頁面**頂部列右側**兩顆按鈕「順勢過濾 ✓/✕」「OI收縮過濾 ✓/✕」。
> **狀態保存**:各自存於瀏覽器 `localStorage`(`regimeFilter` / `qualityFilter`),重新整理/重開仍記得,兩者**預設開**。
> **生效範圍標示**:數據訊號分頁標題會顯示目前生效的過濾(「順 BTC 趨勢」「OI 收縮」);關閉時推薦卡會顯示被過濾的提示(如「BTC 偏空 · 已過濾做多訊號」)。旁邊另有「BTC 趨勢:偏多/偏空」指示燈。

### 1. 順勢過濾(BTC regime)— 預設開
- **規則**:只接「順 BTC 24h 趨勢」的方向訊號(BTC 綠→只做多、BTC 紅→只做空)。
- **依據**:回測逆 BTC 趨勢訊號**淨期望為負**(−1.35% 等);順勢明顯較好。樣本內驗證。

### 2. OI 收縮過濾(品質 gate)— 預設開
- **規則**:只接「**OI 在收縮**(OI 1h < 0)」時的訊號。
- **依據**:**樣本外驗證通過**——OI 收縮(衰竭/平倉)時訊號最賺;OI 擴張(新倉湧入)時幾乎打平(逆勢追高接刀)。
- **品質徽章**:
  - **★優質** = OI 收縮 + 費率極端(樣本外最佳組)
  - **OI↓** = OI 收縮(較可靠)
  - **OI↑** = OI 擴張(追高風險)

> 本質:這兩層是**「在爛行情保護你」**——把虧損壓成打平,不是讓訊號變神。

---

## 八、模擬倉的搭配差異(紀律 vs 動能狙擊)

兩本帳同時跑、即時 A/B(`internal/cache/paper.go`):

| | **紀律倉**(paper) | **動能狙擊倉**(gamble) |
|---|---|---|
| 點火門檻 | **55** | **45**(低門檻) |
| 進場條件 | **需新鮮穿越**(剛啟動已高的不追) | **不需穿越**(直接追已高的) |
| 冷卻 | **4 小時**(方向獨立) | **1 小時** |
| 進場價 | 訊號當下**市價** | 同左 |
| TP / SL | 雷達 fib(0.618R / 0.5R) | 同左 |
| 出場 | TP / SL / 反向訊號平倉 / 24h 逾時 | 同左 |
| 紐約盤 | **不避開**(全時段開新倉) | 同左 |
| 動能燈 | 每筆持倉顯示 🟢動能在/🟡轉弱/🔴熄火(即時雷達分數+CVD) | 同左 |
| 紀錄 | 進場/出場時間、持倉時長、損益、結果 | 同左 |

設計目的:用真實前向資料驗證「紀律 vs FOMO」誰賺。
(備註:早期回測曾佐證「避開紐約盤」較佳,但實盤觀察損單未集中在紐約盤,已改為**全時段開倉**;同向狙擊單已移除。)

---

## 九、回測知識庫(累積至今)

回測工具 `cmd/backtest`:**重放線上同一套評分器**(零漂移),滾動 ~20-25 天。
**核心紀律:回測先行、樣本外驗證、有效才納入、沒用也記原因。**

### ✅ 已採用
- 統一評分器(修掉「負分卻標強力做多」的雙評分器 bug)
- OIHalf 1.5(原 0.3 對 1h OI 雜訊過度反應)
- 流動性抑制、真實 CVD、薄量門檻 100M
- 動能24H、費率反指標、多空比反指標
- 雷達用 **OI 方向**判方向、TP0.618/SL0.5
- **BTC 順勢過濾**(overlay)
- **OI 收縮品質 gate**(overlay,樣本外通過)

### ❌ 停用 / 否決(附原因)
| 項目 | 原因 |
|------|------|
| 1H 動能、相對強弱 | **反指標** |
| ATR、區間壓縮(squeeze) | 當因子無效 |
| 費率 z 分數、大戶比、溢價、現貨 CVD | 無效 |
| OI 加速度、CVD 斜率、多時間框架確認 | **全部反向**(印證評分器是反轉型) |
| 市場廣度過濾 | 不穩、與 BTC regime 重疊 |
| 高槓桿密度 + 紐約盤(合併) | **樣本外過擬合**(更差) |
| 時段獵殺(ICT 銀彈) | 不盡理想,已移除 |
| 快速剝頭皮、社群/DEX 資料 | 扣費後虧損 / 覆蓋率假高 |
| 跨所 OI 一致性 | OKX 無 OI 歷史,**免費 API 測不了** |

### 🔑 最大發現
**這套評分器本質是「反轉/軋空引擎」**:在「**OI 收縮 + 費率極端 + CVD 背離 + 波動偏高**」時最準;在「OI 擴張 + 費率溫和 + 市場平靜(低波動/低 IV)」時最廢。所有「動能確認類」因子都反向,完全自洽。

### ⏳ 已驗證、未上線(待定)
- **DVOL / ATR 低波動 = 廢**:可當全市場 regime 過濾,但與時間段混淆,需更謹慎。
- **CVD 背離、區間壓縮**:方向對但弱/不穩,當參考。

---

## 十、API 端點對照

| 端點 | 對應分頁 |
|------|------|
| `/api/home` | 推薦卡 + 山寨季 + 市場 |
| `/api/oi-cache` | OI 儀表板 / 數據訊號(board) |
| `/api/signals` | 數據訊號(feed 式) |
| `/api/radar` | 爆發雷達 |
| `/api/paper` `/api/gamble` | 模擬倉 / 動能狙擊單 |
| `/api/scorelog` | 訊號紀錄 |
| `/api/risk` | 美股/風險背景燈(含 ±2h 內事件) |
| `/api/events` | 財經事件分頁(完整高影響美國行事曆) |
| `/api/klines` | 個股明細 K 線(`?coin=BTC&interval=1h`,1h/4h/1d) |
| `/api/orderbook` | 盤口大牆/失衡板 |
| `/api/liquidations` | 清算事件 feed + 1h 多空爆倉總額 |
| `/api/coin/{COIN}` | 個股明細抽屜 |
| `/healthz` | 健康檢查 |

---

## 十一、待辦 / 未來方向

1. **Snapshot 落地**(SQLite 或 JSONL):每 15 分鐘存原始評分輸入 + 價格,往未來累積→突破 30 天牆→**跨行情驗證**(目前所有結論的根本限制)。
2. **上伺服器**:Linode Nanode 1GB($5)足夠;**本地 build、只上傳產物**;真正風險是 **Binance 封機房 IP**(需放慢雷達、合併 all-tickers、降併發、429 退避)。
3. 監控顯示候選:OI×價格四象限、波動儀表(ATR%/DVOL regime)、費率極端標記。
4. **代幣解鎖 tab**:卡在資料——DefiLlama 已付費化、免費歷史/即時都 gate。需申請免費金鑰(CoinMarketCal)或付費 API 才能做。
5. **清算 tab**:Binance 歷史端點停用、無免費歷史 → 只能 websocket 往後累積,暫緩。

### 資料可行性備忘(實測 2026-06)
| 想做的 | 免費歷史(可回測) | 免費即時 | 結論 |
|------|------|------|------|
| 代幣解鎖 | ❌ 付費 | ❌ 付費/爬蟲 | 需金鑰 |
| 清算 | ❌ 端點停用 | 🟡 websocket | 只能往後累積 |
| 選擇權 skew/GEX | ❌ 無 | ✅ Deribit | 曾做成 tab,後移除 |
| 宏觀(DXY/股/利率) | 🟡 stooq 擋,**Yahoo 可** | ✅ | **已做成風險背景燈**(即時,非回測) |
| 經濟行事曆(CPI/FOMC) | — | ✅ **faireconomy 免費**(會限流) | **已做成事件倒數**(整合進風險燈) |

> 核心限制:解鎖/清算/宏觀/regime 都是「週–月」級訊號,**即使有資料,25 天回測窗也驗不出來**——這類只能當即時監控,或等 snapshot 累積長歷史。

---

## 公開網頁版 / 帳號分級(Phase 0+1)

把單人工具改成可公開上架的多層級產品。自建 Go auth(無第三方),角色由低到高:`public → member → vip → admin`。

- **登入機制**:bcrypt 雜湊密碼(cost 12,存 `users` 表)+ HMAC-SHA256 簽章 token(`internal/auth`,非 JWT 套件,7 天效期),前端存 `localStorage`,所有請求帶 `Authorization: Bearer`。
- **超級管理員**:首次啟動由 `.env` 的 `ADMIN_USER`/`ADMIN_PASS` 自動建立(neo,admin,不受限);`JWT_SECRET` 務必設長亂碼。
- **角色分級 API**(`server.go` `gate(minRole, handler)`):
  - **public**:`/api/ranking`(多/空 Top10,只給分數**無止盈止損**)、`/api/home`、`/api/events`、`/api/risk`、`/api/orderbook`、`/api/liquidations`
  - **member**:`/api/oi-cache`、`/api/signals`、`/api/radar`、`/api/scorelog`、`/api/klines`、`/api/coin/`
  - **vip**:`/api/paper`、`/api/gamble`(含進場/TP/SL)
  - **admin**:`/api/admin/users`(GET 列表 / POST 新增 / PUT 改角色)+ 以上全部
- **前端**:登入彈窗、token 復原、`can(role)` 控制分頁顯示、公開「綜合排行」分頁、登入/登出列。
- **`users` 表**:username(PK)/pass_hash/role/status(active|pending|banned)/uid/created/expiry/notes。
- **後台管理 UI**:`後台` 分頁(admin 限定)新增使用者、下拉改 role/status、停權;本人列鎖住。

### 伺服器端共用快取(已做,上線前必做)
`internal/cache/ttlcache.go`:singleflight(同 key N 個併發請求只打一次上游)+ TTL + 出錯回傳上次成功值(防交易所封 IP)。包住三個「每請求都打交易所」的端點:**Home 15s / 幣種明細 30s / K 線 30s**;**爆發雷達**加了 compute 鎖(最重的全幣抓取不會被併發重複觸發)。看板/雷達/模擬倉/風險等本來就由背景 ticker 在記憶體快取,與使用者數無關。

### 尚未做(下一階段)
- 註冊流程(Google 表單 → 管理員手動核可設 VIP)、VIP 專區(每週績效圖/獲利單)、入金/UID/交易額驗證(初期人工)。
- ⚠️ 合規:入金/UID/交易額屬交易所聯盟+收費訊號,上線前請諮詢法律意見;全程保留「非投資建議」免責。
